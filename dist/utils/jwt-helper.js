"use strict";
// import jwt from 'jsonwebtoken'
// import env from '../env'
// const jwtTokens = (username: string, email: string, password: string, role: string) =>{
//     const user = {username, email, password, role}
//     const accessToken = jwt.sign(user, env.ACCESS_SECRET_TOKEN, {expiresIn: '2m'})
//     const refreshToken = jwt.sign(user, env.REFRESH_SECRET_TOKEN, {expiresIn: '1d'})
//     return ({accessToken, refreshToken})
// }
// export default jwtTokens
